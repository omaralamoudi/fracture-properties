function filteredImg = ApplyImageFilter(img,kernal)
    filteredImg = zeros(size(img));
    for row = 1:size(filteredImg,1)
        for col = 1:size(filteredImg,2)
            
        end
    end
    
    function pixelIsInternal = NodeIsInternal(node,img)
        
    end
end